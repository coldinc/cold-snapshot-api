{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/exec",                 // ← run an arbitrary command
      "config": { "command": "npm run build" }
    },
    {
      "src": "api/**/*.js",                  // ← files generated by tsup
      "use": "@vercel/node@3.3.0-canary"
    }
  ]
}
